
var left  = keyboard_check(ord("A"))
var right = keyboard_check(ord("D"))
var up    = keyboard_check(ord("W")) 
var down  = keyboard_check(ord("S"))

var move_speed = 5

if left  x -= move_speed
if right x += move_speed
if up    y -= move_speed
if down  y += move_speed
